<div class="book-page">
    <a class="btnVol" href="/books">Voltar</a>
    <div class="container">
        <h1>{{book.title}}</h1>
        <p>O livro tem {{book.nm_paginas}} de páginas.</p>
        <div class="action">
            <a class="btnEdit" href="/books/edit/{{book.id}}">Editar</a>
            <form action="/books/remove{{book.id}}" method="post">
                <input type="submit" value="Remover">
            </form>
        </div>
    </div>
</div>